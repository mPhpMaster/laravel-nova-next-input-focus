<?php

return [
    'enabled' => true,

    /*
    |--------------------------------------------------------------------------
    | Event type to trigger the package
    |--------------------------------------------------------------------------
    | keydown, keyup, ...
    */
    'eventName' => "keydown",

    /*
    |--------------------------------------------------------------------------
    | When pressed ? move to next element
    |--------------------------------------------------------------------------
    | - null to disable
    | **note: use shift key to force move**
    */
    'triggerKey' => "enter",

    /*
    |--------------------------------------------------------------------------
    | When pressed ? trigger form submit
    |--------------------------------------------------------------------------
    | - null to disable
    */
    'submitTriggerKey' => "ctrl+s",

    /*
    |--------------------------------------------------------------------------
    | Watch only these selectors
    |--------------------------------------------------------------------------
    */
    'selectors' => "select,textarea,input",

    /*
    |--------------------------------------------------------------------------
    | Prepend each selector with this
    |--------------------------------------------------------------------------
    */
    'preSelector' => "[index]",
];
